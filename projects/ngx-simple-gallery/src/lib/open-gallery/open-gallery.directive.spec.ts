import { OpenGalleryDirective } from './open-gallery.directive';

describe('OpenGalleryDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenGalleryDirective();
    expect(directive).toBeTruthy();
  });
});
